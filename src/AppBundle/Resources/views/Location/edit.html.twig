{% extends 'PokernightAppBundle::template_default.html.twig' %}

{% block subtitle %}Verwaltung{% endblock %} 

{% block content %}
<div class="container">
	<hr>
	<div class="row">
		<div class="col-sm-12">
		    <h1>Location edit</h1>
		
		    {{ form(edit_form) }}
		
		    <ul class="record_actions">
				<li>
			        <a href="{{ path('location') }}">
			            Back to the list
			        </a>
			    </li>
			    <li>{{ form(delete_form) }}</li>
			</ul>
		</div>
	</div>
</div>
{% endblock %}
